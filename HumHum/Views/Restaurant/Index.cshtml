@model IEnumerable<Shared.RestaurantToReturnDto>

@{
    ViewData["Title"] = "Menu";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}

@* <p class="my-3"> *@
@*     <a asp-action="Create" class="btn btn-primary">Create New</a> *@
@* </p> *@

<section class="menu py-5" id="menu">
    <div class="container">
        <div class="text-center mb-5">
            <span class="d-block fs-8 bg-100">Menu</span>
            <h2 class="fw-bold">Tasty menu of the week</h2>
        </div>

        <div class="row g-4">
            @foreach (var item in Model)
            {
	            <div class="col-sm-6 col-md-4 col-lg-3 h-100 mb-5">
                    <a asp-action="ProductsToResturant" asp-route-id="@item.Id" class="text-decoration-none text-dark">
	                    <div class="card card-span h-100 text-white rounded-3">
                            @* <img src="@item?.Image" class="card-img-top" alt="@item?.Name" style="height: 200px; object-fit: cover;"> *@
                            
                            <img class="img-fluid rounded-3 w-100" style="height:250px;"
                                 src="@item.Image"
                                 @* src="assets/img/gallery/food-world.png"   *@
                                 alt="..."/>
                                 @* style="height: 200px; width: 100%; object-fit: cover;"> *@
								 
                                 <div class="card-img-overlay ps-0">
	                                 <span class="badge bg-primary ms-2 me-1 p-2">
		                                 <i class="fas fa-clock me-1 fs-0"></i><span class="fs-0">Fast</span>
	                                 </span>
                                 </div>
                                 <div class="card-body ps-0">
	                                 <div class="d-flex align-items-center mb-3">
		                                 <div class="flex-1 ms-3">
			                                 <h5 class="mb-0 fw-bold text-1000">@item.Name</h5>
			                                 <span class="text-primary fs--1 me-1"><i class="fas fa-star"></i></span><span class="mb-0 text-primary">4.6</span>
		                                 </div>
	                                 </div>
								 

	                                 <span class="badge bg-soft-danger p-2">
		                                 <span class="fw-bold fs-1 text-danger">Tasty Food</span>
	                                 </span>
                                 </div>
							@* <div class="card-body text-center"> *@
							@* 	<h5 class="card-title">@item?.Name</h5> *@
							@* 	<p class="card-text text-muted mb-2">Tasty Food</p> *@
							@* 	<span class="fw-bold">$XX.XX</span>  *@
							@* 	@* Replace with @item.Price if available *@ 
							@* </div> *@

                        </div>
                    </a>
                </div>
            }
        </div>
    </div>
</section>
